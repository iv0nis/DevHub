- id: TASK-001
  titulo: "Añadir capacidad de crear backlogs en DevAgent"
  descripcion: "Implementar función para generar ítems de backlog a partir de especificaciones dadas."
  estado: P
  prioridad: "{{ Alta | Media | Baja }}"
  valor_negocio: "{{ Beneficio o impacto esperado }}"
  archivos_involucrados:
    - "{{ ruta/archivo_1 }}"
    - "{{ ruta/archivo_2 }}"
  criterios_aceptacion:
    - "{{ Condición 1 de 'hecho' }}"
    - "{{ Condición 2 de 'hecho' }}"
  referencia_blueprint: "{{ US-1.2.3 }}"
  referencia_techspec: "{{ TS-0001 }}"
